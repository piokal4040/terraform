trigger:
- main

# IMPORTANT: You do NOT have Microsoft-hosted agents enabled.
# Replace with your self-hosted agent pool name.
pool:
  name: Default    # ← change ONLY if your agent pool has a different name

steps:
- task: TerraformInstaller@1
  displayName: 'Install Terraform'
  inputs:
    terraformVersion: 'latest'

# Terraform Init
- task: TerraformTaskV4@4
  displayName: 'Terraform Init'
  inputs:
    provider: azurerm
    command: init
    backendServiceArm: ''                        # ← leave empty for managed identity
    backendAzureRmResourceGroupName: my-tfstate-rg
    backendAzureRmStorageAccountName: mytfstateacc
    backendAzureRmContainerName: tfstate
    backendAzureRmKey: terraform.tfstate
    useAzureRmManagedIdentity: true              # Enable managed identity
    managedIdentityClientId: 'c5acaccb-b545-4150-bb66-2d947b28bbd1'  # User-assigned MSI Client ID

# Terraform Plan
- task: TerraformTaskV4@4
  displayName: 'Terraform Plan'
  inputs:
    provider: azurerm
    command: plan
    useAzureRmManagedIdentity: true
    managedIdentityClientId: 'c5acaccb-b545-4150-bb66-2d947b28bbd1'

# Terraform Apply
- task: TerraformTaskV4@4
  displayName: 'Terraform Apply'
  inputs:
    provider: azurerm
    command: apply
    useAzureRmManagedIdentity: true
    managedIdentityClientId: 'c5acaccb-b545-4150-bb66-2d947b28bbd1'
